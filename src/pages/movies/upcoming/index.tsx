import Head from "next/head";
import { SidebarLayout } from "~/components/layout";
import { MovieCategorySection } from "~/components/ui";
import { type NextPageWithLayout } from "../../_app";

const UpcomingPage: NextPageWithLayout = () => {
  return (
    <>
      <Head>
        <title>MovieMania 🎬 - Upcoming</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <>
        <MovieCategorySection title={"Coming Soon"} category={"Coming Soon"} />
      </>
    </>
  );
};

export default UpcomingPage;

UpcomingPage.additionalInfo = {
  getLayout: (page) => (
    <SidebarLayout pageTitle="Choose a movie" backBtn={false}>
      {page}
    </SidebarLayout>
  ),
  requiresAuth: false,
};
